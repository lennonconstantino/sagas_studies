
docker ps
docker stop $(docker ps -aq)
docker container prune -f
docker-compose up --build -d

# mongodb
docker exec -it order-db mongosh "mongodb://admin:123456@localhost:27017"

test> use admin
switched to db admin
admin> show collections
system.users
system.version
admin>
db.order.find()
db.event.find()
db.order.find({"_id":new ObjectId("6516af2f2fc4db317fe7d829")})

# Kafka
localhost:8081/brokers


## Fazendo o build da aplicação
gradle build

docker logs --follow payment-service

# docker utils
docker-compose up --build -d kafka redpanda-console order-db product-db inventory-db payment-db

# swagger
localhost:3000/swagger-ui/index.html


# versionamento
git tag -a v1.4.0 -m "Release 1.4.0 - versao com o inventory-service"
git show v1.4.0
git push origin v1.4.0


